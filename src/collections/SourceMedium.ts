import { CollectionConfig } from 'payload'

export const SourceMedium: CollectionConfig = {
  slug: 'source-media',
  access: {
    read: () => true,
  },
  admin: {
    useAsTitle: 'title',
  },
  fields: [
    {
      name: 'title',
      type: 'text',
      required: true,
    },
    {
      name: 'description',
      type: 'textarea',
    },
  ],
}
